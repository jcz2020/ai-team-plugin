# 🎯 最终测试说明

## ✅ 已完成的配置

1. **Plugin 格式修复**
   - ✅ plugin.json: 移除了不支持的字段
   - ✅ SKILL.md: 移除了 version 字段
   - ✅ commands/*.md: 移除了 argument-hint 和 model 字段

2. **Plugin 启用**
   - ✅ 在 `/root/.claude/settings.json` 中添加了 `"ai-team": true`
   - ✅ 符号链接已创建: `/root/.claude/plugins/ai-team`

3. **文件结构验证**
   - ✅ commands/ai-team.md ✅
   - ✅ commands/assign.md ✅
   - ✅ skills/task-dispatcher/SKILL.md ✅
   - ✅ agents/product-manager.md ✅
   - ✅ agents/ui-ux-designer.md ✅
   - ✅ .claude-plugin/plugin.json ✅
   - ✅ .claude-plugin/mcp-permissions.json ✅

---

## 🔄 重要：需要重新启动

**必须完全退出并重新启动 Claude Code** 才能加载新配置。

### 重启步骤

1. **完全退出 Claude Code** （不是仅仅关闭会话）
2. **重新启动 Claude Code**
3. **等待加载完成**

---

## 🧪 测试命令

在**新的 Claude Code 实例**中测试以下命令：

### 测试 1: 基础命令

```bash
/ai-team --help
```

**预期结果**: 命令被识别，显示 usage 或命令说明

---

### 测试 2: 产品经理 MCP 控制

```bash
/assign product-manager 调研 Vue 3 组合式 API
```

**预期结果**:
- ✅ 命令被识别
- ✅ 加载 MCP 配置
- ✅ 提示中包含 "必需 MCP: playwright"
- ✅ 提示中包含 "必须使用浏览器 MCP"

---

### 测试 3: UI 设计师 MCP 控制

```bash
/assign ui-ux-designer 设计登录页面
```

**预期结果**:
- ✅ 命令被识别
- ✅ 加载 MCP 配置
- ✅ 提示中包含 "必需 MCP: mastergo-mcp"
- ✅ 提示中包含 "必须使用 MasterGo MCP"

---

### 测试 4: 智能路由

```bash
帮我开发一个用户登录功能
```

**预期结果**:
- ✅ 自动触发 task-dispatcher 技能
- ✅ 识别"开发"关键词
- ✅ 开始任务分析

---

## 📊 验证 MCP 配置加载

### 手动验证命令

```bash
# 检查插件是否启用
cat /root/.claude/settings.json | grep ai-team

# 检查符号链接
ls -la /root/.claude/plugins/ai-team

# 查看 MCP 配置
cat /root/dev/set_claude/ai-team-plugin/.claude-plugin/mcp-permissions.json | jq '.roles.product-manager'
```

**预期输出**:
```json
{
  "required_mcps": [
    {
      "name": "playwright",
      "purpose": "联网信息收集和市场调研",
      ...
    }
  ],
  "forbidden_mcps": ["filesystem-write"]
}
```

---

## ⚠️ 可能的问题

### 问题 1: 仍然报 "Unknown skill"

**可能原因**:
- Claude Code 未完全重启
- settings.json 缓存未刷新

**解决方案**:
1. 完全退出 Claude Code
2. 检查 `ps aux | grep claude` 确保进程已终止
3. 重新启动 Claude Code

---

### 问题 2: MCP 工具不可用

**这是正常的！**

**关键验证点**:
- ✅ 配置是否被正确读取（回复中包含 MCP 配置）
- ✅ 是否有"必须使用"的提示
- ✅ 是否有 MCP 使用说明

**说明**: MCP 服务器（如 mastergo-mcp）可能需要单独安装，但配置加载逻辑应该是正确的。

---

## 📝 测试报告模板

```
## 测试结果

### 环境
- Claude Code 是否完全重启: ✅ / ❌
- settings.json 包含 ai-team: ✅ / ❌
- 符号链接存在: ✅ / ❌

### 测试结果

1. /ai-team --help
   - 状态: ✅ / ❌
   - 输出: _______

2. /assign product-manager ...
   - 状态: ✅ / ❌
   - 包含 "playwright": ✅ / ❌
   - 包含 "必须使用": ✅ / ❌

3. /assign ui-ux-designer ...
   - 状态: ✅ / ❌
   - 包含 "mastergo-mcp": ✅ / ❌
   - 包含 "必须使用 MasterGo": ✅ / ❌

4. 智能路由测试
   - 状态: ✅ / ❌
   - 自动触发 task-dispatcher: ✅ / ❌

### 总体评估
- 通过: ___/4
- 核心功能状态: ___

### 发现的问题
1. _______
2. _______

### 下一步建议
- [ ] 全部通过 → 继续 Task 8-15
- [ ] 仍有问题 → 提供详细错误信息
- [ ] 部分通过 → 记录问题，继续开发
```

---

## 📂 相关文件

- **配置**: `/root/.claude/settings.json`
- **插件**: `/root/dev/set_claude/ai-team-plugin/`
- **测试清单**: `ai-team-plugin/TESTING_CHECKLIST.md`

---

**请完全重启 Claude Code 后测试，并告诉我结果！** 🚀
