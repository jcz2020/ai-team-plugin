# MCP Permissions 配置说明

本文档详细说明 `mcp-permissions.json` 配置文件的结构、字段含义和使用方法。

## 📋 目录

- [配置文件概述](#配置文件概述)
- [文件结构](#文件结构)
- [角色权限配置](#角色权限配置)
- [MCP 工具注册表](#mcp-工具注册表)
- [全局设置](#全局设置)
- [安全规则](#安全规则)
- [验证工具](#验证工具)
- [最佳实践](#最佳实践)

---

## 配置文件概述

`mcp-permissions.json` 是 AI Team Plugin 的核心配置文件，用于控制每个专业角色可以使用的 MCP（Model Context Protocol）工具。

### 核心目标

1. **权限细粒度控制** - 每个角色只能使用授权的 MCP 工具
2. **强制关键流程** - 某些角色必须使用特定的工具（如产品经理必须用浏览器调研）
3. **安全防护** - 禁止角色执行超出其职责范围的操作（如禁止直接推送到主分支）
4. **灵活配置** - 支持必需/可选/禁止三级权限控制

### 配置文件位置

```
ai-team-plugin/.claude-plugin/mcp-permissions.json
```

---

## 文件结构

```json
{
  "$schema": "./mcp-permissions-schema.json",
  "version": "1.0.0",
  "description": "MCP 工具权限配置 - 控制每个角色可以使用的 MCP 工具",
  "last_updated": "2026-01-28",
  "roles": {
    "role-key": {
      "display_name": "角色显示名称",
      "required_mcps": [...],
      "optional_mcps": [...],
      "forbidden_mcps": [...],
      "notes": "角色权限说明"
    }
  },
  "global_settings": {
    "strict_mode": true,
    "fallback_behavior": "warning",
    "audit_log": true,
    "permission_levels": {...},
    "safety_rules": [...],
    "mcp_tool_registry": {...}
  }
}
```

---

## 验证工具

### 使用验证脚本

```bash
./scripts/validate-mcp-permissions.sh
```

---

**版本**: 1.0.0
**最后更新**: 2026-01-28
