---
description: 分配任务给特定角色
argument-hint: <角色名称> <任务描述>
allowed-tools: [Task, Read, Glob]
model: sonnet
---

# 分配任务给角色

## 参数

$ARGUMENTS

**格式**: `<角色名>` `<任务描述>`

示例: `/assign product-manager 调研 Next.js 14 最佳实践`

---

## 执行流程

### 1. 解析参数

从 `$ARGUMENTS` 提取:
- **角色名称**: 第一个词（如 `product-manager`）
- **任务描述**: 剩余部分

### 2. 验证角色

从 `.claude-plugin/mcp-permissions.json` 读取 `roles[<角色名>]`。

如果不存在，提示可用角色:
- `product-manager`: 产品经理
- `ui-ux-designer`: UI/UX 设计师
- `developer`: 开发工程师
- `security-expert`: 安全专家

### 3. 构建 Prompt

```
你是 {{角色名称}} subagent。

## 任务

{{任务描述}}

## MCP 权限

### 必需 (必须使用)
{{#each required_mcps}}
- **{{name}}**: {{purpose}} ({{tools}})
  - 行为: {{fallback_behavior}}
  - 指令: {{instructions}}
{{/each}}

### 可选 (按需使用)
{{#each optional_mcps}}
- **{{name}}**: {{purpose}} ({{tools}})
{{/each}}

### 禁止 (禁止使用)
{{#each forbidden_mcps}}
- ❌ {{this}}
{{/each}}

## 要求

1. 必须使用所有必需 MCP
2. 禁止使用任何禁止 MCP
3. 完成后报告: 工作内容、使用的工具、产出的文件、遇到的问题
```

### 4. 调用 Subagent

```
Task:
  subagent_type: "general-purpose"
  prompt: "[上面构建的 prompt]"
```

### 5. 报告结果

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  📋 任务分配完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 👤 角色
{{角色名称}}

## 📝 任务
{{任务描述}}

## ✅ 完成的工作
{{subagent 输出}}

## 📁 产出的文件
{{文件列表}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 错误处理

- **角色不存在**: 提示可用角色列表
- **MCP 不可用**: 按 `fallback_behavior` 处理（error/warning）
- **Subagent 失败**: 显示错误并询问是否重试
