# 命令识别测试指南

> **测试目标**: 验证 AI Team Plugin 的命令能够被 Claude Code 正确识别和执行
>
> **测试范围**: `/ai-team` 和 `/assign` 两个核心命令
>
> **测试维度**: 命令识别、参数解析、MCP 配置加载、错误处理

---

## 📋 测试准备

### 前置条件检查清单

在执行测试前，请确认以下条件已满足：

- [ ] **Marketplace 已添加**: `/plugin marketplace add /root/dev/set_claude/ai-team-marketplace`
- [ ] **插件已安装**: `/plugin install ai-team`
- [ ] **插件已启用**: 在 Settings > Plugins 中确认 ai-team 已启用
- [ ] **配置文件存在**: 验证 `.claude-plugin/plugin.json` 和 `mcp-permissions.json` 存在
- [ ] **MCP 服务可用**: 确认 playwright 等 MCP 已配置并可用

### 验证安装状态

```bash
# 检查插件是否安装到缓存目录
ls -la ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/

# 预期输出应包含:
# commands/
# skills/
# agents/
# templates/
# .claude-plugin/
```

---

## 🧪 测试场景

### 场景 1: 基础命令识别测试

#### 目标
验证命令能被 Claude Code 识别，不会出现 "Unknown skill" 或 "Unknown command" 错误

#### 测试步骤

**1.1 测试 `/ai-team` 命令识别**

在 Claude Code 中执行：
```
/ai-team --help
```

**预期结果**:
- ✅ 命令被识别，没有错误提示
- ✅ 显示命令内容或执行流程
- ❌ **失败**: 出现 "Unknown skill" 或类似错误

**验收标准**:
```markdown
- 命令识别: ✅ 通过 / ❌ 失败
- 响应时间: < 2 秒
- 错误信息: 无错误
```

---

**1.2 测试 `/assign` 命令识别**

在 Claude Code 中执行：
```
/assign product-manager 测试任务
```

**预期结果**:
- ✅ 命令被识别
- ✅ 参数解析成功（`product-manager` 和 `测试任务`）
- ✅ 触发 Task tool 调用
- ❌ **失败**: 参数解析错误或未识别命令

**验收标准**:
```markdown
- 命令识别: ✅ 通过 / ❌ 失败
- 参数解析: ✅ 正确 / ❌ 错误
- 角色验证: ✅ 通过 / ❌ 失败
```

---

#### 失败诊断

如果命令未识别，检查以下项目：

1. **Plugin 配置**
   ```bash
   cat /root/dev/set_claude/ai-team-marketplace/plugins/ai-team/.claude-plugin/plugin.json | jq .
   ```
   - 确认 `name` 字段为 `ai-team`
   - 确认 `version` 字段存在且为 `1.0.0`

2. **Marketplace 配置**
   ```bash
   cat /root/dev/set_claude/ai-team-marketplace/.claude-plugin/marketplace.json | jq .
   ```
   - 确认 `plugins` 数组包含 `ai-team`

3. **命令文件存在性**
   ```bash
   ls -la ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/
   ```
   - 应包含 `ai-team.md` 和 `assign.md`

4. **Frontmatter 格式**
   ```bash
   head -5 ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/ai-team.md
   ```
   - 确认包含 `description` 和 `allowed-tools` 字段
   - 确认没有不支持的字段（如 `argument-hint`, `model`）

---

### 场景 2: 参数解析测试

#### 目标
验证命令能正确解析参数和用户输入

#### 测试步骤

**2.1 单参数测试**

执行：
```
/assign product-manager 测试单一参数
```

**预期行为**:
- ✅ 角色名: `product-manager`
- ✅ 任务描述: `测试单一参数`
- ✅ 正确加载 product-manager 的 MCP 配置

**验收标准**:
```markdown
- 角色识别: ✅ product-manager / ❌ 其他
- 任务描述: ✅ 完整 / ❌ 截断
- MCP 加载: ✅ 成功 / ❌ 失败
```

---

**2.2 多参数测试**

执行：
```
/assign developer 修复登录页面的验证逻辑 bug，要求支持邮箱和手机号
```

**预期行为**:
- ✅ 角色名: `developer`
- ✅ 任务描述: `修复登录页面的验证逻辑 bug，要求支持邮箱和手机号`
- ✅ 任务描述完整（未截断）

**验收标准**:
```markdown
- 角色识别: ✅ developer / ❌ 其他
- 任务完整性: ✅ 完整 / ❌ 截断
- 特殊字符处理: ✅ 正确 / ❌ 错误
```

---

**2.3 特殊字符测试**

执行：
```
/assign ui-ux-designer 设计"暗黑模式"切换功能，参考 https://example.com
```

**预期行为**:
- ✅ 正确处理引号
- ✅ 正确处理 URL
- ✅ 正确处理中英文混合

**验收标准**:
```markdown
- 引号处理: ✅ 正确 / ❌ 转义错误
- URL 处理: ✅ 完整 / ❌ 截断
- 中文支持: ✅ 正常 / ❌ 乱码
```

---

**2.4 空参数测试**

执行：
```
/assign
```

**预期行为**:
- ✅ 提示正确的使用格式
- ✅ 显示可用角色列表
- ❌ 不应该崩溃或显示晦涩错误

**验收标准**:
```markdown
- 错误提示: ✅ 友好 / ❌ 晦涩
- 使用帮助: ✅ 显示 / ❌ 缺失
- 角色列表: ✅ 完整 / ❌ 缺失
```

---

### 场景 3: MCP 配置加载测试

#### 目标
验证命令能正确加载角色的 MCP 权限配置

#### 测试步骤

**3.1 必需 MCP 加载测试**

执行：
```
/assign product-manager 调研 AI 编码工具市场
```

**预期行为**:
- ✅ 从 `mcp-permissions.json` 读取 `product-manager` 的配置
- ✅ 识别 `required_mcps` 包含 `playwright`
- ✅ 在 prompt 中包含 MCP 使用指令

**验证方法**:
```markdown
检查 Subagent 的 prompt 是否包含:

## MCP 工具权限

### 必需 MCP (必须使用)
- **playwright**: 进行联网信息收集和市场调研
  - 行为: error
  - 指令: 调研时必须使用浏览器 MCP 访问真实网站
```

**验收标准**:
```markdown
- 配置读取: ✅ 成功 / ❌ 失败
- 必需 MCP 识别: ✅ playwright / ❌ 缺失
- Prompt 包含: ✅ 完整 / ❌ 缺失
```

---

**3.2 禁止 MCP 加载测试**

执行：
```
/assign product-manager 撰写产品需求文档
```

**预期行为**:
- ✅ 识别 `forbidden_mcps` 包含写入类工具
- ✅ 在 prompt 中明确禁止使用这些工具
- ✅ Subagent 拒绝执行文件写入操作

**验证方法**:
```markdown
检查 prompt 是否包含:

### 禁止 MCP (禁止使用)
- ❌ Write
- ❌ Edit
- ❌ Bash (写入类命令)
```

**验收标准**:
```markdown
- 禁止列表识别: ✅ 正确 / ❌ 缺失
- Prompt 包含: ✅ 完整 / ❌ 缺失
- Subagent 遵守: ✅ 遵守 / ❌ 违规
```

---

**3.3 可选 MCP 加载测试**

执行：
```
/assign architect 设计系统架构
```

**预期行为**:
- ✅ 识别 `optional_mcps` 列表
- ✅ 在 prompt 中标记为可选
- ✅ Subagent 可以根据需要选择是否使用

**验收标准**:
```markdown
- 可选列表识别: ✅ 正确 / ❌ 缺失
- 标记为可选: ✅ 正确 / ❌ 误导
- Subagent 判断: ✅ 合理 / ❌ 混乱
```

---

**3.4 MCP 配置文件验证**

验证 `mcp-permissions.json` 格式正确：

```bash
# 检查 JSON 格式
cat /root/dev/set_claude/ai-team-marketplace/plugins/ai-team/.claude-plugin/mcp-permissions.json | jq .

# 预期输出示例:
{
  "roles": {
    "product-manager": {
      "required_mcps": [
        {
          "name": "playwright",
          "purpose": "进行联网信息收集和市场调研",
          "tools": ["browser_navigate", "browser_snapshot"],
          "fallback_behavior": "error",
          "instructions": "调研时必须使用浏览器 MCP 访问真实网站"
        }
      ],
      "optional_mcps": [...],
      "forbidden_mcps": [...]
    }
  }
}
```

**验收标准**:
```markdown
- JSON 格式: ✅ 有效 / ❌ 无效
- 必需字段: ✅ 完整 / ❌ 缺失
- 角色定义: ✅ 存在 / ❌ 缺失
```

---

### 场景 4: 错误处理测试

#### 目标
验证命令在异常情况下的错误处理能力

#### 测试步骤

**4.1 无效角色测试**

执行：
```
/assign invalid-role 测试任务
```

**预期行为**:
- ✅ 提示角色不存在
- ✅ 显示可用角色列表
- ✅ 不执行任何操作
- ❌ 不应该崩溃或显示技术栈错误

**验收标准**:
```markdown
- 错误提示: ✅ 友好 / ❌ 技术性
- 角色列表: ✅ 显示 / ❌ 缺失
- 安全性: ✅ 未执行 / ❌ 已执行
```

---

**4.2 MCP 不可用测试**

临时禁用某个 MCP（如 playwright），然后执行：

```
/assign product-manager 调研任务
```

**预期行为**:
- ✅ 检测到 MCP 不可用
- ✅ 根据 `fallback_behavior` 处理：
  - `error`: 立即停止并报错
  - `warning`: 记录警告但继续执行
- ✅ 提示用户解决方案

**验收标准**:
```markdown
- 错误检测: ✅ 成功 / ❌ 未检测
- Fallback 行为: ✅ 符合配置 / ❌ 不一致
- 用户提示: ✅ 清晰 / ❌ 晦涩
```

---

**4.3 权限不足测试**

模拟 MCP 权限不足场景（如果 MCP 需要认证）：

```
/assign product-manager 需要认证的调研任务
```

**预期行为**:
- ✅ 捕获权限错误
- ✅ 提示用户配置 MCP
- ✅ 提供配置指南链接

**验收标准**:
```markdown
- 错误捕获: ✅ 成功 / ❌ 未捕获
- 配置指引: ✅ 清晰 / ❌ 缺失
- 友好度: ✅ 友好 / ❌ 技术性
```

---

### 场景 5: 集成测试

#### 目标
验证命令在真实工作流中的端到端功能

#### 测试步骤

**5.1 完整工作流测试**

执行：
```
/ai-team 开发一个用户登录功能
```

**预期行为**:
1. ✅ 产品经理 subagent 被调用
2. ✅ 需求分析完成
3. ✅ 架构师 subagent 被调用
4. ✅ 开发工程师 subagent 被调用
5. ✅ 每个阶段的 MCP 配置正确加载
6. ✅ 最终验收完成

**验收标准**:
```markdown
- 工作流完整性: ✅ 完整 / ❌ 中断
- Subagent 调用: ✅ 成功 / ❌ 失败
- MCP 配置: ✅ 正确 / ❌ 错误
- 最终产出: ✅ 符合预期 / ❌ 不完整
```

---

**5.2 并发命令测试**

快速连续执行多个命令：

```
/assign developer 任务1
/assign designer 任务2
/assign tester 任务3
```

**预期行为**:
- ✅ 每个命令独立处理
- ✅ 无状态冲突
- ✅ 参数不混淆

**验收标准**:
```markdown
- 独立性: ✅ 独立 / ❌ 干扰
- 参数隔离: ✅ 隔离 / ❌ 混淆
- 执行顺序: ✅ 正确 / ❌ 混乱
```

---

## 📊 测试报告模板

### 测试执行记录

```markdown
# 命令识别测试报告

**测试日期**: YYYY-MM-DD
**测试版本**: 1.0.0
**测试环境**: Claude Code [版本]
**操作系统**: Linux/macOS/Windows

---

## 测试结果汇总

| 场景 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| 场景 1 | 命令识别 | ✅/❌ | |
| 场景 1 | 参数解析 | ✅/❌ | |
| 场景 2 | 单参数 | ✅/❌ | |
| 场景 2 | 多参数 | ✅/❌ | |
| 场景 2 | 特殊字符 | ✅/❌ | |
| 场景 3 | 必需 MCP | ✅/❌ | |
| 场景 3 | 禁止 MCP | ✅/❌ | |
| 场景 3 | 可选 MCP | ✅/❌ | |
| 场景 4 | 无效角色 | ✅/❌ | |
| 场景 4 | MCP 不可用 | ✅/❌ | |
| 场景 5 | 完整工作流 | ✅/❌ | |
| 场景 5 | 并发命令 | ✅/❌ | |

**通过率**: XX/12 (XX%)

---

## 发现的问题

### 问题 1: [问题描述]
- **严重程度**: 高/中/低
- **重现步骤**: [步骤]
- **实际结果**: [结果]
- **预期结果**: [结果]
- **相关文件**: [文件路径]

### 问题 2: [问题描述]
...

---

## 改进建议

1. [建议 1]
2. [建议 2]
3. [建议 3]

---

## 测试结论

- [ ] 测试通过，可以发布
- [ ] 需要修复问题后重新测试
- [ ] 需要优化性能
- [ ] 需要改进文档

---

**测试人员**: [姓名]
**审核人员**: [姓名]
```

---

## 🔧 自动化测试脚本

### 快速验证脚本

```bash
#!/bin/bash
# 命令识别快速验证脚本

echo "🧪 AI Team Plugin 命令识别测试"
echo "================================"

# 颜色定义
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

# 测试计数
PASSED=0
FAILED=0

# 测试函数
test_case() {
  local name=$1
  local command=$2
  local expected=$3

  echo -n "测试: $name ... "

  if eval "$command" | grep -q "$expected"; then
    echo -e "${GREEN}✅ 通过${NC}"
    ((PASSED++))
  else
    echo -e "${RED}❌ 失败${NC}"
    ((FAILED++))
  fi
}

# 测试 1: 插件安装检查
test_case \
  "插件已安装到缓存目录" \
  "ls ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/" \
  "ai-team.md"

# 测试 2: plugin.json 格式
test_case \
  "plugin.json 格式正确" \
  "cat ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/.claude-plugin/plugin.json | jq .name" \
  "ai-team"

# 测试 3: 命令文件存在
test_case \
  "assign.md 命令文件存在" \
  "ls ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/" \
  "assign.md"

# 测试 4: mcp-permissions.json 格式
test_case \
  "mcp-permissions.json 格式正确" \
  "cat ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/.claude-plugin/mcp-permissions.json | jq .roles" \
  "product-manager"

# 测试 5: frontmatter 字段检查
test_case \
  "ai-team.md 包含必需 frontmatter" \
  "head -3 ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/ai-team.md" \
  "description"

# 测试 6: 不包含不支持的字段
echo -n "测试: 命令文件不包含不支持的字段 ... "
if ! head -10 ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/commands/ai-team.md | grep -q "argument-hint\|model:"; then
  echo -e "${GREEN}✅ 通过${NC}"
  ((PASSED++))
else
  echo -e "${RED}❌ 失败${NC}"
  ((FAILED++))
fi

# 测试 7: 角色定义存在
test_case \
  "产品经理角色定义存在" \
  "ls ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/agents/" \
  "product-manager.md"

# 测试 8: 技能文件存在
test_case \
  "task-dispatcher 技能存在" \
  "ls ~/.claude/plugins/cache/ai-team-marketplace/ai-team/1.0.0/skills/task-dispatcher/" \
  "SKILL.md"

# 汇总
echo ""
echo "================================"
echo -e "${GREEN}通过: $PASSED${NC}"
echo -e "${RED}失败: $FAILED${NC}"
echo "总计: $((PASSED + FAILED))"
echo ""

if [ $FAILED -eq 0 ]; then
  echo -e "${GREEN}🎉 所有测试通过！${NC}"
  exit 0
else
  echo -e "${YELLOW}⚠️  存在失败的测试，请检查${NC}"
  exit 1
fi
```

**使用方法**:
```bash
# 保存脚本
chmod +x /root/dev/set_claude/ai-team-plugin/tests/test-command-recognition.sh

# 执行测试
./test-command-recognition.sh
```

---

## 📝 测试检查清单

### 手动测试清单

执行测试时，请逐项检查：

**基础功能**
- [ ] `/ai-team` 命令被识别
- [ ] `/assign` 命令被识别
- [ ] 帮助信息正确显示
- [ ] 参数解析正确

**参数处理**
- [ ] 单参数正常工作
- [ ] 多参数正常工作
- [ ] 特殊字符正确处理
- [ ] 中文正常处理
- [ ] 空参数有友好提示

**MCP 配置**
- [ ] 必需 MCP 正确加载
- [ ] 可选 MCP 正确加载
- [ ] 禁止 MCP 正确识别
- [ ] MCP 权限正确传递给 subagent

**错误处理**
- [ ] 无效角色有友好提示
- [ ] MCP 不可用有清晰错误信息
- [ ] 权限不足有配置指引

**工作流集成**
- [ ] 完整工作流可以执行
- [ ] 多个 subagent 正确调用
- [ ] 检查点正常工作
- [ ] 最终验收完成

---

## 🚨 常见问题排查

### 问题 1: 命令未被识别

**症状**: 执行命令时出现 "Unknown skill" 错误

**排查步骤**:
1. 确认 marketplace 已添加: `/plugin marketplace list`
2. 确认插件已安装: `/plugin list`
3. 确认插件已启用: Settings > Plugins
4. 重启 Claude Code
5. 检查命令文件 frontmatter 格式

---

### 问题 2: 参数解析错误

**症状**: 角色名或任务描述不正确

**排查步骤**:
1. 检查 `$ARGUMENTS` 变量是否正确传递
2. 检查参数分隔逻辑
3. 测试不同格式的输入
4. 查看日志中的实际参数值

---

### 问题 3: MCP 配置未加载

**症状**: Subagent 未收到 MCP 权限信息

**排查步骤**:
1. 验证 `mcp-permissions.json` 存在
2. 检查 JSON 格式是否正确
3. 验证角色名称匹配
4. 检查文件读取权限
5. 查看命令日志

---

### 问题 4: Subagent 未调用

**症状**: 命令执行但没有 subagent 响应

**排查步骤**:
1. 确认 Task tool 可用
2. 检查 prompt 构建是否正确
3. 验证 subagent_type 设置
4. 查看 Claude Code 日志
5. 测试简化版本的 prompt

---

## 📚 相关文档

- [AI Team Plugin 完整实现计划](/root/dev/set_claude/docs/plans/2026-01-28-ai-team-plugin-fix-and-complete.md)
- [产品经理角色测试指南](/root/dev/set_claude/ai-team-plugin/tests/product-manager-test.md)
- [MCP 权限配置文档](/root/dev/set_claude/ai-team-plugin/docs/MCP_PERMISSIONS.md)
- [结构验证文档](/root/dev/set_claude/ai-team-plugin/docs/STRUCTURE_VALIDATION.md)

---

**最后更新**: 2026-01-28
**维护人员**: AI Team Plugin 开发团队
