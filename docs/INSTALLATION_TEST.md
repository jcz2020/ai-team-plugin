# AI Team Plugin - 安装测试指南

本文档提供 AI Team Plugin 的完整安装、测试和验证流程。

---

## 📋 目录

- [前置要求](#前置要求)
- [安装方法](#安装方法)
- [安装验证](#安装验证)
- [功能测试](#功能测试)
- [故障排除](#故障排除)
- [测试报告](#测试报告)

---

## 前置要求

### 系统要求

- **操作系统**: Linux, macOS, Windows (WSL2)
- **Claude Code**: 已安装并配置
- **Git**: 用于克隆仓库
- **Bash**: 用于运行验证脚本

### Claude Code 配置

确保 `~/.claude/settings.json` 中启用了插件：

```json
{
  "plugins": [
    {
      "name": "ai-team",
      "enabled": true
    }
  ]
}
```

### 可选工具

- **jq**: JSON 处理工具（推荐安装）
  ```bash
  # macOS
  brew install jq

  # Linux
  apt-get install jq

  # 验证安装
  jq --version
  ```

---

## 安装方法

### 方法 1: 符号链接（推荐开发环境）

**优点**: 修改立即生效，便于开发调试

```bash
# 1. 确保插件目录存在
mkdir -p ~/.claude/plugins

# 2. 创建符号链接
ln -s /root/dev/set_claude/ai-team-plugin ~/.claude/plugins/ai-team

# 3. 验证链接
ls -la ~/.claude/plugins/ | grep ai-team
# 输出: lrwxrwxrwx ... ai-team -> /root/dev/set_claude/ai-team-plugin
```

### 方法 2: 复制（推荐生产环境）

**优点**: 独立副本，不影响源码

```bash
# 1. 复制插件到 Claude 插件目录
cp -r /root/dev/set_claude/ai-team-plugin ~/.claude/plugins/ai-team

# 2. 验证文件
ls ~/.claude/plugins/ai-team
```

### 方法 3: Git Clone（从远程仓库）

```bash
# 1. 克隆仓库
cd ~/.claude/plugins
git clone https://github.com/your-org/ai-team-plugin.git ai-team

# 2. 验证克隆
cd ai-team && git status
```

---

## 安装验证

### 快速验证

```bash
# 运行综合验证脚本
cd /root/dev/set_claude/ai-team-plugin
./scripts/verify-plugin.sh --quick
```

**预期输出**:
```
========================================
  AI Team Plugin 安装验证 (快速模式)
========================================

✓ Plugin 配置文件存在
✓ MCP 权限配置存在
✓ README.md 文件存在
✓ 命令文件完整 (2/2)
✓ 技能文件完整 (1/1)
✓ 角色定义文件 (2/11)

安装状态: ✓ 部分成功
建议: 继续实现剩余角色定义
```

### 完整验证

```bash
# 运行完整验证（包含详细报告）
./scripts/verify-plugin.sh --full

# 生成 JSON 格式报告
./scripts/verify-plugin.sh --json > verification-report.json
```

### 手动验证步骤

#### 1. 检查目录结构

```bash
# 验证关键目录
ls -la ~/.claude/plugins/ai-team/.claude-plugin/
ls -la ~/.claude/plugins/ai-team/agents/
ls -la ~/.claude/plugins/ai-team/commands/
ls -la ~/.claude/plugins/ai-team/skills/
```

**预期输出**:
```
.claude-plugin/
├── plugin.json
├── mcp-permissions.json
└── mcp-permissions-schema.json

agents/
├── product-manager.md
└── ui-ux-designer.md

commands/
├── ai-team.md
└── assign.md

skills/
└── task-dispatcher/
    └── SKILL.md
```

#### 2. 验证配置文件

```bash
# 验证 plugin.json
cat ~/.claude/plugins/ai-team/.claude-plugin/plugin.json | jq .

# 验证 mcp-permissions.json
cat ~/.claude/plugins/ai-team/.claude-plugin/mcp-permissions.json | jq .version
```

**预期输出**:
```json
{
  "name": "ai-team",
  "version": "1.0.0",
  "description": "AI 专业团队 - 通过对话式智能调度协调多个专业 subagent 协作开发",
  "author": {
    "name": "Your Name",
    "email": "your@email.com"
  }
}
```

#### 3. 检查 Claude Code 识别

**方法 1: 检查命令是否可用**

在 Claude Code 对话中输入：
```
/ai-team --help
```

**预期响应**: 命令被识别并显示帮助信息

**方法 2: 检查技能是否加载**

```
/skills list
```

**预期响应**: 应该看到 `task-dispatcher` 技能

---

## 功能测试

### 测试用例 1: 简单任务（单个角色）

**测试命令**:
```
/ai-team 分析当前项目的目录结构
```

**预期流程**:
1. 产品经理角色被激活
2. 分析项目目录
3. 生成结构报告

**验证点**:
- [ ] 命令被正确识别
- [ ] 产品经理角色被激活
- [ ] 输出结构化报告
- [ ] 没有错误或异常

### 测试用例 2: 中等任务（2-3 个角色）

**测试命令**:
```
/ai-team 设计一个简单的 To-Do List API
```

**预期流程**:
1. 产品经理 - 需求分析
2. 架构师 - API 设计
3. 开发工程师 - 实现建议

**验证点**:
- [ ] 多个角色按顺序激活
- [ ] 每个角色完成其职责
- [ ] 输出连贯的设计方案
- [ ] 角色切换平滑

### 测试用例 3: MCP 工具强制使用

**测试命令**:
```
/ai-team 调研最新的前端框架趋势
```

**预期流程**:
1. 产品经理角色被激活
2. **必须**使用 Playwright MCP 进行浏览器调研
3. 收集真实数据而非凭空猜测

**验证点**:
- [ ] Playwright MCP 被调用
- [ ] 浏览器操作被执行
- [ ] 调研数据真实可信
- [ ] 引用了来源

### 测试用例 4: UI 设计任务

**测试命令**:
```
/ai-team 设计一个现代化登录页面
```

**预期流程**:
1. UI/UX 设计师角色被激活
2. **必须**使用 MasterGo MCP 创建设计稿
3. 生成设计规范和组件说明

**验证点**:
- [ ] MasterGo MCP 被调用
- [ ] 设计稿被创建
- [ ] 包含设计规范
- [ ] 组件说明详细

### 测试用例 5: 智能路由

**测试命令**:
``/ai-team {不同复杂度的任务}``

**测试场景**:

| 复杂度 | 任务示例 | 预期行为 |
|--------|----------|----------|
| 简单 | 修复一个 bug | 单个角色（开发工程师） |
| 中等 | 添加一个功能 | 2-3 个角色 |
| 复杂 | 开发完整模块 | 完整工作流 |

**验证点**:
- [ ] 智能路由正确判断任务复杂度
- [ ] 分配合适数量的角色
- [ ] 工作流程高效
- [ ] 没有冗余步骤

---

## 故障排除

### 问题 1: 命令不被识别

**症状**: 输入 `/ai-team` 显示"未知命令"

**诊断步骤**:
```bash
# 1. 检查插件目录
ls ~/.claude/plugins/ai-team/commands/

# 2. 验证命令文件
cat ~/.claude/plugins/ai-team/commands/ai-team.md

# 3. 检查 settings.json
cat ~/.claude/settings.json | jq '.plugins'
```

**解决方案**:
1. 确保插件目录路径正确
2. 重启 Claude Code
3. 检查 `settings.json` 中 `enabled: true`

### 问题 2: MCP 权限配置无效

**症状**: 角色无法访问 MCP 工具

**诊断步骤**:
```bash
# 1. 验证 JSON 语法
cat ~/.claude/plugins/ai-team/.claude-plugin/mcp-permissions.json | jq .

# 2. 运行验证脚本
cd ~/.claude/plugins/ai-team
./scripts/validate-mcp-permissions.sh
```

**解决方案**:
1. 修复 JSON 语法错误
2. 确保所有必需字段存在
3. 检查 MCP 工具名称拼写

### 问题 3: 符号链接失效

**症状**: 文件找不到或权限错误

**诊断步骤**:
```bash
# 1. 检查链接状态
ls -la ~/.claude/plugins/ai-team

# 2. 验证源路径
readlink ~/.claude/plugins/ai-team

# 3. 检查源目录
ls -ld /root/dev/set_claude/ai-team-plugin
```

**解决方案**:
1. 删除失效链接: `rm ~/.claude/plugins/ai-team`
2. 重新创建链接: `ln -s /root/dev/set_claude/ai-team-plugin ~/.claude/plugins/ai-team`
3. 或改用复制方式安装

### 问题 4: 技能不加载

**症状**: `/skills` 命令看不到 `task-dispatcher`

**诊断步骤**:
```bash
# 1. 检查技能文件
ls -la ~/.claude/plugins/ai-team/skills/task-dispatcher/

# 2. 验证 SKILL.md 格式
head -20 ~/.claude/plugins/ai-team/skills/task-dispatcher/SKILL.md
```

**解决方案**:
1. 确保 SKILL.md frontmatter 正确
2. 重启 Claude Code
3. 检查技能文件路径

### 问题 5: 角色定义缺失

**症状**: 提示"角色定义文件不存在"

**诊断步骤**:
```bash
# 1. 列出所有角色文件
ls ~/.claude/plugins/ai-team/agents/

# 2. 对比配置
cat ~/.claude/plugins/ai-team/.claude-plugin/mcp-permissions.json | jq '.roles | keys'
```

**解决方案**:
1. 确保角色文件存在
2. 文件名与配置中的 role_key 匹配
3. 检查文件权限

---

## 测试报告

### 报告模板

```markdown
# AI Team Plugin 测试报告

**测试日期**: 2026-01-28
**测试人员**: [Your Name]
**插件版本**: 1.0.0

## 安装验证

- [ ] 目录结构完整
- [ ] 配置文件正确
- [ ] 命令可用
- [ ] 技能加载

## 功能测试

| 测试用例 | 状态 | 备注 |
|---------|------|------|
| 简单任务 | ✓ 通过 | - |
| 中等任务 | ✓ 通过 | - |
| MCP 强制使用 | ✓ 通过 | - |
| UI 设计任务 | ✓ 通过 | - |
| 智能路由 | ✓ 通过 | - |

## 发现问题

1. [问题描述]
   - 严重程度: 高/中/低
   - 复现步骤: ...
   - 临时解决方案: ...

## 总体评估

- 安装难度: ⭐⭐☆☆☆
- 功能完整性: ⭐⭐⭐⭐☆
- 文档质量: ⭐⭐⭐⭐⭐
- 推荐指数: ⭐⭐⭐⭐☆
```

### 生成自动化报告

```bash
# 运行验证并生成报告
./scripts/verify-plugin.sh --report > test-report-$(date +%Y%m%d).md

# 查看报告
cat test-report-$(date +%Y%m%d).md
```

---

## 持续监控

### 自动化测试脚本

创建定时任务，定期验证插件状态：

```bash
# 添加到 crontab
crontab -e

# 每天早上 9 点运行验证
0 9 * * * cd ~/.claude/plugins/ai-team && ./scripts/verify-plugin.sh --quiet
```

### 健康检查端点（可选）

如果使用 CI/CD，可以配置健康检查：

```yaml
# .github/workflows/health-check.yml
name: Plugin Health Check

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run verification
        run: ./scripts/verify-plugin.sh --full
```

---

## 最佳实践

### 开发环境

1. 使用符号链接安装
2. 频繁运行验证脚本
3. 修改后立即测试
4. 保持测试文档更新

### 生产环境

1. 使用复制方式安装
2. 运行完整验证后上线
3. 定期健康检查
4. 保留测试报告备查

### 团队协作

1. 统一安装方法
2. 共享测试脚本
3. 维护测试用例库
4. 定期同步测试结果

---

## 附录

### A. 验证脚本选项

```bash
./scripts/verify-plugin.sh [OPTIONS]

选项:
  --quick      快速验证（基础检查）
  --full       完整验证（包含详细报告）
  --json       输出 JSON 格式
  --report     生成 Markdown 报告
  --quiet     静默模式（仅输出错误）
  --help       显示帮助信息
```

### B. 测试检查清单

- [ ] 插件已正确安装
- [ ] 所有配置文件有效
- [ ] 命令可以被识别
- [ ] 技能可以加载
- [ ] 角色定义完整
- [ ] MCP 权限配置正确
- [ ] 简单任务测试通过
- [ ] 中等任务测试通过
- [ ] 复杂任务测试通过
- [ ] MCP 工具强制使用有效
- [ ] 智能路由工作正常
- [ ] 错误处理正确
- [ ] 日志记录完整

### C. 常用命令

```bash
# 验证安装
./scripts/verify-plugin.sh

# 检查 MCP 权限
./scripts/validate-mcp-permissions.sh

# 测试命令
/ai-team --help

# 列出技能
/skills list

# 查看日志
tail -f ~/.claude/logs/ai-team.log
```

---

**文档版本**: 1.0.0
**最后更新**: 2026-01-28
**维护者**: AI Team Plugin Team
